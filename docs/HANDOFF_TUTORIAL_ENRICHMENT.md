# HANDOFF — Tutorial Enrichment + Editor Tooling + Authoring Guide

Concise handoff for any agent continuing this task. User request: (1) flesh out the 5-room
Factory tutorial (feels hollow), (2) add Godot editor tooling for human content authoring,
(3) write an authoring guide WITH screenshots (real in-game renders + ASCII editor diagrams).

## STATUS

### DONE — Phase 1: additive Item `.tres` loader (verified)

- `components/items/ItemDatabase.cs`: `All` is now non-readonly; added `LoadExternalItems()`
  which merges `res://resources/items/*.tres` into `All` (built-ins always win; duplicates skipped+warned).
- `boot/GameInit.cs`: calls `ItemDatabase.LoadExternalItems()` in `BootDeferred` (top, after logger init).
- `resources/items/factory_coffee.tres` (Consumable, Heal 15, icon_0011) and
  `resources/items/grease_can.tres` (Consumable, Heal 20, icon_0006) — generated by
  `tests/GenerateSampleItems.cs` (`godot --headless --path . --script res://tests/GenerateSampleItems.cs`).
- Proof wired: `OpeningZone.tscn` has `CoffeePickup` (instance `PickupItem.tscn`,
  `ItemId="factory_coffee"`, Sprite2D texture `assets/items/icons/icon_0011.png`) at (256, 232).
- `VerifyAllLevels` PASSES after this.

### DONE — Phase 2: 4 GDScript editor plugins (parse-checked via `godot --headless --quit`, zero errors)

All registered in `project.godot` line `enabled=PackedStringArray(...)`:

1. `addons/level_wizard/` (plugin.gd + level_factory.gd + plugin.cfg) — "New Level" dock.
   Shared helper `level_factory.gd` → static `LevelFactory.create_level(name, tileset_path, music_path, ambience_path)`
   builds a scene from `res://levels/BaseLevel.tscn` + 2 TileMapLayers (`ArchitectureTilemap`,
   `ForegroundTilemap`) with factory_tileset, saves to `res://levels/<slug>/maps/<Name>.tscn`, opens it.
2. `addons/level_assembly/plugin.gd` — expanded: "New Level…" popup (calls LevelFactory), new
   COMPONENTS entries: "Readable Object" (`components/npcs/ReadableObject.tscn`),
   "NPC (Dialog)" (`components/npcs/DialogBranchTrigger.tscn`), "New Quest…" status hint.
3. `addons/transition_audit/` — dock: scans edited scene for `FloorSwitch.TargetDoorPath`,
   `TeleportPad.TargetPadPath`, `LevelTransition.TargetTransitionName`; dropdown targets,
   Validate dead links, Apply wiring via UndoRedo.
4. `addons/content_editors/` — Item Editor dock (New/Save/Refresh → `resources/items/*.tres`)
   and Quest Editor dock (Id/Title/Desc/StartFlag + Objectives; Save → `resources/quests/*.tres`;
   "Register in QuestManager" appends to `autoload/QuestManager.tscn` `Quests` array, append-only).
   NOTE: a one-off "Parse error" report for this file was a stale artifact; `--quit` is clean.

### DONE — groundwork for Phase 4

- `tests/ScreenshotTool.cs` WORKS. Run WITHOUT `--headless` (renders via DISPLAY):
  `godot --path . --script res://tests/ScreenshotTool.cs`
  → writes 2560x1440 PNGs of the 5 rooms to `docs/AUTHORING_GUIDE/assets/shots/<Room>.png`.
  (RID-leak errors at exit are harmless.) Re-run after enrichment.

## REMAINING WORK

### Phase 3 — Enrich the 5 rooms (NOT STARTED)

HARD CONSTRAINTS:

- NEVER rename these stable nodes (VerifyFactoryExpansion + transitions depend on them):
  `TimeClock, SortingFloorEntrance, ClockOutReturn, FactoryJamitor, FactoryCrate,
  FactoryPressurePlate, CrateGate, AssemblyLineEntrance, SortingFloorArrival, ShutdownChecklist,
  MaintenancePadWest/East, AssemblyLineExit, AssemblyLineArrival, SequencePlateA/B/C,
  SequenceController, InspectionDoor, InspectionApproved, EggdropSoupPickup, LoadingBayEntrance,
  ControlRoomReturn, LoadingTimedGate, LoadingTimedSwitchWest/East, ArrestCutscene, OfficerBacon,
  EggsileTransition, HubArrival, CoreTilemapLayer, ArchitectureTilemap, ForegroundTilemap`.
- NEVER touch `tile_map_data`. Only ADD new nodes at the END of each .tscn (+ new ext_resource lines
  before the first `[sub_resource`). Path-only `ext_resource` (no uid) is fine for textures/scripts.
- After edits: `dotnet build` (0 NEW warnings), `VerifyAllLevels.cs`, `VerifyFactoryExpansion.cs` — all must pass.

STEP 3a — Generate 5 DialogBranch `.tres` via C# (nested sub-resources must NOT be hand-authored):
Write `tests/GenerateDialogBranches.cs` (pattern-copy `tests/GenerateSampleItems.cs`), run headless.
API: `DialogBranch.Nodes` = `Godot.Collections.Array<DialogNode>`; `DialogNode{Id, SpeakerName,
Lines(string[]), Responses(DialogResponse[]), SetFlagsOnEnter(string[])}`;
`DialogResponse{Text, NextNodeId, SetFlagOnSelect}` (empty NextNodeId = end).
Shape per NPC: start node (Id "start", SetFlagsOnEnter=[flag], 2 choice responses → "opt0"/"opt1",
- "(Leave)" response with NextNodeId=""), then opt0/opt1 nodes each with lines + "(Leave)".
Save to `res://levels/factory/npcs/<Name>Dialog.tres`:
| File | Speaker | Flag | Start lines | Opt0 | Opt1 |
|---|---|---|---|---|---|
| GreeterDialog | Greeter | met_greeter | new-egg welcome; clock out at TimeClock | "How do I play?" → WASD/Shift/Space/E/F | "Egg-costume rumors?" → vague phantom hint |
| PipDialog | Pip | met_pip | crate must go on pressure plate to open gate | "Any tips?" → push crate onto plate | "Egg phantom?" → HR is weird about it |
| BoltDialog | Bolt | met_bolt | conveyors carry west / sign checklist | "How do conveyors work?" → ride or sprint against | "Egg phantom?" → egg-shaped footprints |
| VexDialog | Vex | met_vex | plates in order A,B,C opens door | "After inspection?" → Loading Bay, don't get arrested | "The 'egg' situation?" → cooperate with Bacon |
| CraneDialog | Crane | met_crane | two timed switches, 5 seconds; Bacon sees you out | "Bacon seems intense." → by the book | "Egg phantom?" → pretend you didn't see |

STEP 3b — Create `levels/factory/npcs/GenericFactoryWorker.tscn` (hand-authored .tscn is fine):
Mirror `levels/factory/npcs/FactoryJamitor.tscn` exactly, with changes:
root `StaticBody2D` named `GenericFactoryWorker`; child `Sprite2D` default texture
`res://assets/characters/Joe.png` (instances override); child `DialogBranchTrigger` =
instance of `res://components/npcs/DialogBranchTrigger.tscn` with
`script = ExtResource(.../DialogBranchTrigger.cs)` (same pattern Jamitor uses for CutsceneTrigger)
- `InteractionCollision` CircleShape2D r=55; root `PhysicsCollision` Rect 44x40 at (0,8);
`AnimationPlayer` with the same 2-track idle bob animation (copy verbatim from FactoryJamitor.tscn)
autoplay `idle`. No DialogBranch baked (instances override). Drop the shadow Polygon2D.

STEP 3c — Per-room additions (add at END of each map .tscn; positions are open-floor coords near landmarks):

| Room | NPC instance (override Sprite2D texture + DialogBranchTrigger.DialogBranch) | pos | Decor (Sprite2D) | ReadableObject (instance `components/npcs/ReadableObject.tscn`, set DialogLines, add Sprite2D child w/ texture) | Pickup (instance `components/items/PickupItem.tscn`, ItemId + Sprite2D icon) |
| --- | --- | --- | --- | --- | --- |
| OpeningZone | Greeter (Joe.png, GreeterDialog) | (450,120) | crate.png (520,180), pipe_junction.png (-40,-140) | "Break Room Rules" warning_sign.png (300,-40) | — (CoffeePickup already added) |
| SortingFloor | Pip (Frank.png, PipDialog) | (100,80) | crate.png stack x2 (-200,120), clipboard.png (-300,-100) | "Sorting Safety" warning_sign.png (250,-80) | egg_salad_sandwich + icon_0004 (60,160) behind crate |
| AssemblyLine | Bolt (GrandpaSmith.png, BoltDialog) | (-100,-100) | conveyor.png (0,-160), pipe_junction.png (300,100) | "Conveyor Safety" warning_sign.png (200,-160) | egg_nog + icon_0009 (400,-40) |
| ControlRoom | Vex (Oatmeal.png, VexDialog) | (200,60) | control_console.png (150,-100), manifest.png (-100,120) | "Employee of the Month" warning_sign.png (-40,-140) | deviled_egg + icon_0002 (480,60) near soup alcove |
| LoadingBay | Crane (Milk.png, CraneDialog) | (300,-40) | crate.png (100,120), manifest.png (420,140) | "WANTED: egg-costumed figure" warning_sign.png (0,-200) | hardboiled_egg + icon_0000 (600,-100) |
Plus per-room AUDIO — edit the root node's existing `LevelMusic`/`LevelAmbience` ExtResource refs:
OpeningZone keep `lvl_0_the_tutorial` + `factory_afterhours_ambient`; SortingFloor →
`lvl_2_the_village`; AssemblyLine → `generic1.ogg`; ControlRoom → `lvl_1_the_royal_palace`;
LoadingBay → `lvl_8_the_volcanic_sea_shore` + ambience `prison_night_ambient`.
(All under `assets/audio/music/…`; swap the ext_resource path/uid on the root node only.)
Atmosphere: add one `PointLight2D` per room (energy ~0.6; warm color in break areas,
cool/blue in LoadingBay) — built-in node, no script needed.

### Phase 4 — Screenshots

Re-run `godot --path . --script res://tests/ScreenshotTool.cs` after Phase 3. Optionally extend
`Scenes[]` for close-ups. PNGs land in `docs/AUTHORING_GUIDE/assets/shots/`.

### Phase 5 — Guide

`docs/AUTHORING_GUIDE/index.md`: 7 topics (new level+tilemap, NPCs+dialog, quests, items, music,
puzzles, +extras: flags conventions, save points, verification, debugging) with embedded room
screenshots (`![](.../assets/shots/X.png)`) and ASCII box diagrams for editor panels (Level
Assembly dock, Level Wizard, Content Editors, Cutscene Inspector, transition_audit). Include a
"what NOT to hand-author" section (nested-subresource .tres) and reference the 4 new plugins.

### Phase 6 — Verify + ship
NOTE (2026-08-24 hotfix): NPC dialogs are wired via `GenericFactoryWorker.DialogBranchPath`
(string export, resolved in C# at runtime) — NOT via a typed `DialogBranch` scene property,
which throws InvalidCastException in the editor's import scan (runs before the C# assembly loads).

`dotnet build` → `godot --headless --path . --script res://tests/VerifyAllLevels.cs` →
`... res://tests/VerifyFactoryExpansion.cs` → per-plugin GDScript parse check:
`godot --headless --path . --check-only --script res://addons/<plugin>/<script>.gd`
(⚠️ `godot --headless --quit` does NOT parse editor dock scripts — `--check-only --script` is the only reliable gate; grep case-insensitively, Godot prints "Parse Error" with capital E).
File GitHub issue, commit to main with `Closes #N`, push.

## KEY FACTS

- Levels: `Node2D` root + `levels/BaseLevel.cs` (exports LevelName/LevelMusic/LevelAmbience);
  tilemaps `ArchitectureTilemap`+`ForegroundTilemap` w/ `assets/tilemaps/factory_tileset.tres`.
- NPC pattern = `FactoryJamitor.tscn`. Dialog multi-choice = `DialogBranchTrigger` + `DialogBranch` .tres.
- ReadableObject exports: `DialogLines`(string[]), `AlternateLines`, `GateFlag`, `Once`.
- PickupItem exports: `ItemId`, `Count`, `DialogLines`, `SetFlag`; sprite is a manual Sprite2D child.
- Item icons: `assets/items/icons/icon_0000..0029.png`. Character PNGs available: Jamitor, OfficerBacon,
  Frank, GrandpaSmith, Oatmeal, Eggbert, Eggbert2, Milk, Joe.
- GDScript only in `addons/`; C# only for game code. Never hand-author nested-subresource .tres.
